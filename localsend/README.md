This crate is vendored from [localsend](https://github.com/localsend/localsend) `packages/core` (Apache-2.0, see LICENSE).
It is trimmed to the protocol parts sendme needs: discovery (multicast), the v2 HTTP/HTTPS client and server, and crypto. The unfinished v3 transport, WebRTC, and examples were removed.
